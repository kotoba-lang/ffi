;; nbb.edn -- the kbb engine reads :deps HERE, not from deps.edn. :paths and
;; the kotoba.test sha are copied from deps.edn.
{:paths ["src"]
 :deps {io.github.kotoba-lang/test {:git/url "https://github.com/kotoba-lang/test.git"
                                    :git/sha "2ab4885f99fd25552d380d0f6b0b54c5a72eb7ae"}}}
